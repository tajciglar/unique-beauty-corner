{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "prisma-generate",
			"command": "npx",
			"args": [
				"prisma",
				"generate"
			],
			"options": {
				"cwd": "${workspaceFolder}/backend"
			},
			"presentation": {
				"reveal": "silent"
			}
		}
	]
}